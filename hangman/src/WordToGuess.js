import React from 'react';

function WordToGuess() {

    const [word_to_guess_length, set_word_to_guess_length] = React.useState(["trains", "acoustics",
            "level", "sneaky", "legs", "needless", "crabby", "bone", "use", "charming", "crow", "stage"])
    const [list_of_preset_words] = React.useState([])

    const create_word_to_guess = () => {

    }

    return(
        <>

        </>
    )

}